/**
 * renderer.js — DOM Rendering Layer
 * ============================================================
 * [B1] ไฟล์นี้สร้างไว้สำหรับ STEP B5
 * จะย้าย renderQueue(), renderUploadedFiles(), showPreview()
 * ออกจาก app.js มาไว้ที่นี่ทั้งหมด
 *
 * ตอนนี้ยังว่างอยู่ — app.js ยังคง render logic เดิมอยู่ทั้งหมด
 * ============================================================
 */
