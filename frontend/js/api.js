/**
 * api.js — API Layer (fetch wrappers)
 * ============================================================
 * [B1] ไฟล์นี้สร้างไว้สำหรับ STEP B4
 * จะย้าย uploadBatch() และ startPolling() ออกจาก app.js
 * รวมทุก fetch() call ไว้ที่นี่ที่เดียว
 *
 * ตอนนี้ยังว่างอยู่ — app.js ยังคง API logic เดิมอยู่ทั้งหมด
 * ============================================================
 */
